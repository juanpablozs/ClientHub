# ClientHub (monorepo)

Mini SaaS para gestión de clientes y proyectos.

Estructura inicial creada: monorepo con `apps/api`, `apps/web` y `packages/shared`.

Siguientes pasos: implementar API con Prisma y autenticación, luego frontend.
# ClientHub