# Database (Prisma) - host for local development (connects to MySQL on localhost)
# If you plan to run the DB in Docker and run Prisma from your host, use the localhost form below.
# If you run Prisma inside the `api` container, see `apps/api/.env.docker` which uses the service name `mysql`.
DATABASE_URL="mysql://user:password@127.0.0.1:3306/clienthub"

# JWT
JWT_ACCESS_TOKEN_SECRET=replace_access_secret
JWT_REFRESH_TOKEN_SECRET=replace_refresh_secret
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# App
PORT=4000
FRONTEND_URL=http://localhost:5173
